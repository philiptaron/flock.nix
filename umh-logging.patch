diff --git a/kernel/umh.c b/kernel/umh.c
index 1b13c5d34624..5098753d99bf 100644
--- a/kernel/umh.c
+++ b/kernel/umh.c
@@ -46,6 +46,18 @@ static void call_usermodehelper_freeinfo(struct subprocess_info *info)
 
 static void umh_complete(struct subprocess_info *sub_info)
 {
+	if (sub_info->retval == 0) {
+		pr_info("umh: successfully ran %s with args", sub_info->path);
+	} else {
+		pr_notice("umh: exit code %d from %s with args",
+			sub_info->retval, sub_info->path);
+	}
+
+	for (unsigned i = 0; sub_info->argv[i] != NULL; ++i) {
+		pr_cont(" %s", sub_info->argv[i]);
+	}
+	pr_cont("\n");
+
 	struct completion *comp = xchg(&sub_info->complete, NULL);
 	/*
 	 * See call_usermodehelper_exec(). If xchg() returns NULL
